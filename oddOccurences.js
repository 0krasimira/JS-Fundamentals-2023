function partyTime (arr) {

    let vipList = []
    let regularList = []

    for (const line of arr) {
        let list = line.split(', ')
        let index = 0
        let command = index[list]
        index++
        while(command !== 'PARTY') {
            if(command.startsWith)

        command = index[list]
        index++
        }
    console.log(list)
    }

}

partyTime (['7IK9Yo0h',
'9NoBUajQ',
'Ce8vwPmE',
'SVQXQCbc',
'tSzE5t0p',
'PARTY',
'9NoBUajQ',
'Ce8vwPmE',
'SVQXQCbc'
])