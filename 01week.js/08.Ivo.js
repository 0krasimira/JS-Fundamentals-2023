function multiplicationTable (number, multiplier) {

    
        for (let times = 1; times <= 10; times++) {
            console.log(`${number} X ${times} = ${number * times}`)
        }
    

    }


multiplicationTable (5)